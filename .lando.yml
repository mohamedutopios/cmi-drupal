name: drupal-cmi-demo
recipe: drupal11
config:
  php: "8.3"
  via: apache:2.4
  database: mysql:8.0
  composer_version: 2-latest
  drush: ^13
  webroot: web
services:
  appserver:
    overrides:
      environment:
        DRUSH_OPTIONS_URI: "https://drupal-cmi-demo.lndo.site"